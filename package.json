{
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  },
  "scripts": {
    "test": "npx jest --coverage",
    "coverage": "npx jest --coverage",
    "perf:bench": "node test/perf/benchmark_findSimilarIds.js"
  }
}
